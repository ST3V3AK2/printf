#ifndef PRINTF_PROTOTYPES
#define PRINTF_PROTOTYPES

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>

int _putchar(char c);
int _puts(char *str);
int _printf(const char *format, ...);

#endif
